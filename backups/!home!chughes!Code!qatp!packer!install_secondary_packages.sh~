#!/bin/bash

YUM_INSTALLS="wget screen lsof emacs nc ack"
APT_INSTALLS="wget screen lsof emacs netcat ack-grep redis-cli"

if [ `ls /etc/debian_version` ]; then
    sudo apt-get -y install $APT_INSTALLS;
elif [ `ls /redhat-release` ]; then
    sudo yum -y install $YUM_INSTALLS;
fi;
